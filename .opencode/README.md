# .opencode README - CLO

Skeleton mirrors JV OS capability structure, zero JV OS or Legends content copied.

- `agents/` - new agents prefixed `clo-*.md`
- `skills/` - new skills in `clo-*/SKILL.md`
- `commands/` - project slash commands
- `plans/` - plan files
- `package.json` - pinned to same `@opencode-ai/plugin` version as JV OS / Legends for capability parity

Isolation: never copy `client_secret*.json`, `.env`, or JV OS / Legends brand/pipeline content here.
